<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a2dam.sqlite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".IntroEstudiantes" />
        <activity android:name=".IntroProfesores" />
        <activity android:name=".DelEstudiantes" />
        <activity android:name=".DelProfesores" />
        <activity android:name=".VerEstudiantes" />
        <activity android:name=".FiltradoUsuarios" />
        <activity android:name=".OpAsignaturas" />
        <activity android:name=".VerAsignaturas"></activity>
    </application>

</manifest>